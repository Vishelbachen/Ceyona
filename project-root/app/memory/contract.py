MEMORY_SCHEMA = {
    "user_memory": {
        "fields": [
            "user_id",
            "content",
            "mem_type",     # user / assistant / system
            "importance",   # 0.0 - 1.0
            "created_at"
        ]
    }
}